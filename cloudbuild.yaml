steps:
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t'
      - 'gcr.io/$PROJECT_ID/${_SERVICE_NAME}'
      - '-f'
      - 'api/Dockerfile'
      - '.'
images:
  - 'gcr.io/$PROJECT_ID/${_SERVICE_NAME}'
